spring:
  config:
    import: aws-parameterstore:/param/reverse-auction/prod/

  datasource:
    url: ${POSTGRES_URL}
    username: ${POSTGRES_USERNAME}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    show-sql: false
    properties:
      hibernate:
        ddl-auto: validate
        jdbc:
          time_zone: Asia/Seoul
        format_sql: false

  cloud:
    # AWS 관련
    aws:
      # parameterstore 활성화
      parameterstore:
        enabled: true
      region:
        static: ap-northeast-2

  data:
    redis:
      ssl:
        enabled: ${SPRING_DATA_REDIS_SSL_ENABLED:false}
      protocol: ${REDIS_PROTOCOL:redis}
      host: ${REDIS_HOST:localhost}
      port: 6379
aws:
  eventbridge:
    lambda-arn: ${LAMBDA_ARN}
    role-arn: ${ROLE_ARN}
    legacy-lambda-arn: ${LEGACY_LAMBDA_ARN}
  s3:
    bucket: ${S3_BUCKET_NAME}         # 이미지 업로드 대상 S3 버킷 이름
  cloudfront:
    domain: ${CLOUDFRONT_DOMAIN}      # 이미지 조회용 CDN 도메인 (예: https://d1234abcdef.cloudfront.net)

# actuator 관련
management:
  server:
    port: 9001
    address: 0.0.0.0  # ECS awsvpc — Prometheus 컨테이너가 별도 IP에서 접근하므로 필요
  endpoints:
    web:
      exposure:
        include:
          - health
          - prometheus

  endpoint:
    health:
      show-details: never
    prometheus:
      enabled: true
